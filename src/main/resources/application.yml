spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ficWriter
    username: ficWriter
    password: P@ssword123drowss@P
    initialization-mode: always
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        ddl-auto: create-drop
        default_schema: ficWriter
#        hbm2ddl:
#          import_files: data/data-dev.sql

