spring:
  profiles:
    active: db-mysql
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        hbm2ddl:
          import_files: data/actor.sql, data/article.sql, data/actor_state.sql, data/book.sql
logging:
  level:
    org:
      springframework: INFO

