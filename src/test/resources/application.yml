spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ficwriter-test
    username: dl
    password: p@ssword
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        hbm2ddl:
          import_files: data/actor.sql, data/article.sql, data/actor_state.sql
logging:
  level:
    org:
      springframework: INFO

